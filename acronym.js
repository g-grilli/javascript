acronym = ['very', 'important', 'person']

acronym.reduce(function (x, y) {
  return x + y;
});
function pickles(words) {
	for (var i = 0; words.length; i++) {
		var res = words.slice(0, 1)
		return res;
	}
}